export default defineAppConfig({
    nuxtIcon: {
      size: '20px', // default <Icon> size applied
      class: 'Icon',
      color:'gray', // default <Icon> class applied
      aliases: {
        'nuxt': 'logos:nuxt-icon',
      }
    }
  })